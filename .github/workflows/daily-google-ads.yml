name: Google Ads Daily ETL

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"  # Every day at 03:00 UTC (08:30 IST)

jobs:
  google_ads_d1:
    uses: ./.github/workflows/_reusable-etl.yml
    with:
      pipeline: google
      group: d1
    secrets: inherit
