name: Facebook Ads Daily ETL - d1c

on:
  workflow_dispatch:
  schedule:
    - cron: "10 3 * * *" # Every day at 03:10 UTC

jobs:
  facebook_etl:
    strategy:
      matrix:
        group: [d1c]
      fail-fast: false
    uses: ./.github/workflows/_reusable-etl.yml
    with:
      pipeline: facebook
      group: ${{ matrix.group }}
    secrets: inherit
