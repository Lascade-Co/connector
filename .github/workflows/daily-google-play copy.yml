name: App Store Connect Daily ETL

on:
  workflow_dispatch:
  schedule:
    - cron: "10 6 * * *"  # Every day at 06:10 UTC

jobs:
  app_store_etl:
    uses: ./.github/workflows/_reusable-etl.yml
    with:
      pipeline: app_store
      group: d1
    secrets: inherit
