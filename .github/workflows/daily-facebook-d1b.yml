name: Facebook Ads Daily ETL - d1b

on:
  workflow_dispatch:
  schedule:
    - cron: "10 2 * * *" # Every day at 02:10 UTC

jobs:
  facebook_etl:
    strategy:
      matrix:
        group: [d1b]
      fail-fast: false
    uses: ./.github/workflows/_reusable-etl.yml
    with:
      pipeline: facebook
      group: ${{ matrix.group }}
    secrets: inherit
