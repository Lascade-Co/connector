name: Facebook Ads Daily ETL - Batch A

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *" # Every day at 00:00 UTC (05:30 IST)

jobs:
  facebook_etl:
    strategy:
      matrix:
        group: [d1a, m4a, d2a]
      fail-fast: false
      max-parallel: 1
    uses: ./.github/workflows/_reusable-etl.yml
    with:
      pipeline: facebook
      group: ${{ matrix.group }}
    secrets: inherit
