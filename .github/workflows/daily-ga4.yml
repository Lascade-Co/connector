name: GA4 Daily ETL

on:
  workflow_dispatch:
  schedule:
    - cron: "30 1 * * *"  # Every day at 01:30 UTC

jobs:
  ga4_d1:
    uses: ./.github/workflows/_reusable-etl.yml
    with:
      pipeline: google_analytics
      group: d1
    secrets: inherit
